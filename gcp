#include <bits/stdc++.h>
#define all(a) a.begin(),a.end()
#define endl "\n"
#define u_map unordered_map
#define ll long long

using namespace std;

void solve() {
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int T = 1, i = 0;
    cin >> T;
    while (T--) {
        cout << "Case #" << ++i << ": ";
        solve();
    }
    return 0;
}
